from django.apps import AppConfig

class PrescricaoAppConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "prescricao_app"
    verbose_name = "Prescrições (Receitas)"
